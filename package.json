{
  "name": "giffcash",
  "version": "0.1.0",
  "private": true,
  "description": "A dependency-free lending operations MVP built with Node.js.",
  "type": "module",
  "scripts": {
    "dev": "node --watch server.js",
    "start": "node server.js",
    "test": "node --test",
    "check": "node --check server.js && node --check src/app.js && node --check public/app.js"
  },
  "engines": {
    "node": ">=20"
  }
}
